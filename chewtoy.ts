// Give eslint something to chew on to validate the config
// Run `yarn validate`, you should get no errors or warnings, if you uncomment the lines below
// you should get a warning about the unused variable, double import, quotes and redundant type

// import fs from 'fs';
// import fs2 from 'fs';

// const bar: string = "foo";
const foo = 'bar';
console.log(foo);
